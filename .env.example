# PostgreSQL connection string used by Prisma
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/studyos?schema=public"

# Auth.js secret: run `openssl rand -base64 32` or `npx auth secret`
AUTH_SECRET="replace-with-a-long-random-secret"

# NextAuth public URL
AUTH_URL="http://localhost:3000"
